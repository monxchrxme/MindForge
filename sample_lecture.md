# Указатели - работа с данными вне области видимости💀
Указатель - тип данных. Используются для хранения адресной информации. Указатель - целое число.
Конкретные значения адресов программисту не нужен(если не системное программирование)
Адрес состоит из 2х элементов: селектор(раньше сегмент) и числовое смещение
define pagesize - в среднем 4 килобайта
## Синтаксис
``` C
int *a, *b; // лучше так, тк понятнее, что для каждой переменной
int* a;

int a[] // объявление массива

void *a // нетипизированный указатель, напрямую к адресу.
```

# Для чего
1. Передача массивов из функции
``` C
void f(int *m, int n) // передача массива в функцмю

main()
{
	int a[10];
	f(a, 10);
}
```
2. Динамическое управление памятью
	1. Статический массив - после размещения в памяти управляется полностью ОС и ЯП, программист не имеет отношения. Размер не изменить
	2. Динамический массив - управляет программист, Выделение, освобождение, изменение размера памяти.
	- Статическая память - 16 mb на программу, она чрезвычайно нужна для ОС, при превышении - конец. А динамики - сколько угодно. int m[n] - нельзя
# Обращение
``` C
int m[10];
*m = 0; m[0] = 0; // одно и то же

m+1 // адрес ячейки памяти, сдвинутый на 1 элемент(int = 4 байта)
*(m+1) = 1; // второй элемент массива

// в цикле
*(m+i) = m[i]

scanf("%d", m+i);

// могут быть отрицательные индексы
m++; // тогда 1 элемент m[-1]
```

# Операции
- Прибавление и вычитание целых чисел = сдвиг указателя влево, вправо
	Квадратные скобки - коротко
- Вычитание указателей друг из друга = расстояние указателями в элементах
``` C
int m[10], a;
int *p1, *p2;
p1 = &m[3];
p2 = m+7;
a = p1-p2; // -4
```

- Сравнение = какой дальше

# Динамическая память БАЗА
- malloc() - выделяет память в байтах, возвращает адрес
- calloc() - заполнение памяти сразу (медленный malloc)
- realloc() - изменение размера памяти 
- free() - освобождение памяти, к уже освобожденной памяти нельзя, но работать с освоб памятью можно - парная для малока Тестирование программы на утечки valgrind😂
- memset()
Объем памяти в байтах
``` C
int *ptr;
ptr = (int*)malloc(n*sizeof(int)); // явное приведение типов
```
---
``` C
f(int **m, ...)
f(int *m[], ...)
f(int m[][]) // нельзя
```

## Область видимости и время жизни
- Область видимости переменной - тот участок (блок) кода программы, из которого к переменной можно обратиться по ее имени. Но тк блок кода выполняется за время

- Время жизни - то время, в течение работы программы, когда переменная размещена в памяти и с ней можно работать

## Локальные и глобальные переменные
Приоритет отдается локальным переменным
- Локальные - область видимости ограничены блоком кода
- Глобальные - видна во всем файле

# Классы памяти
- auto - почти все локальные переменные
- register - Рекомендация процессору не выгружать из кэша процессора (самая быстрая память)
- extern -
	- к локальной - почти статик
	- к глобальной, то видна в любом файле программы
- static -
	- локальная - время жизни на всю программу
	- глобальная - 

# Указатели на функцию
указатель представляет из себя прототип функции - поздняя привязка late binding.
``` C
double f2(double x, double (*f) (double)) // скобки при *f - обязательно
{ 
	return f(x) * f(x);
}

int main()
{
	double a;
	a = f2(0, sin);//0
	a = f2(25, sqrt); // 25
	a = f2(0, printf()) // не будет, тк не тот тип
}
```

# Структуры данных
Структура - тип данных, позволяющий хранить в себе разносортные данные, не бывает void, нельзя хранить функции (указатели на функции можно), не может быть пустой

``` C
struct _demo
{
	// поля структуры
	int a, b; 
	float c;
	int d;
};

int main()
{
	struct _demo d1, d2;
	d1.a = 10;
	d2.a = 15;
	...
	scanf("%f", &d2.c)
}
```

``` C
typedef strict _point
{
	double x, y;
} Point2D;

int main()
{
	Point2D p1, p2, points[10];
}

```

``` C
struct point
{
	float x, y;
	struct point foo;
};
struct point *ptr;
ptr = (struct point*)malloc(sizeof(struct point));
ptr->x=1;
ptr->y=-1;
ptr->foo.x=10;
free(ptr);
```
---
# Книги
- Технические подробности про ram - understanding the linux kernel - Даниэль Баве, Марк Чизатти
---
# Дз
- Вся память выделяется в main 🎶
- 2 Функции вычисления определителя 2 и 3 порядка 

